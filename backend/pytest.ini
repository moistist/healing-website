[pytest]
# pytest 配置文件：告诉 pytest 去哪里找测试，并让 import 走项目根目录。
testpaths = tests
# 将 backend/ 加入 sys.path，使得 conftest 与测试文件能直接
# ``from main import app``、``from database import get_db`` 等。
pythonpath = .
# 详细输出 + 显示 traceback 风格
addopts = -ra --tb=short
